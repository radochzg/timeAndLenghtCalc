public class unitCalculator {
    public static void main(String[] args) {
        LengthUnit lengthUnit = new LengthUnit();
        lengthUnit.meter(5.2);
        TimeUnit timeUnit = new TimeUnit();
        timeUnit.hour(14);
    }
}
